_: {
  sys = {
    dev.enable = true;

    desktop = {
      enable = true;

      chat = {
        discord.enable = true;
        matrix.enable = true;
      };
    };

    fonts.enable = true;
    podman.enable = true;

    shell.enable = true;
  };
}
