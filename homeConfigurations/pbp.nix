_: {
  sys = {
    dev.enable = true;

    desktop = {
      enable = true;

      defaultBroswer = "firefox";

      discord = true;
      element = true;
    };

    fonts.enable = true;
    podman.enable = true;

    shell.enable = true;
  };
}
